# Core runtime
langgraph>=0.2
pandas>=2.0
rich>=13.0
python-dotenv>=1.0

# Live Grok reasoning (falls back to deterministic logic if the API is unreachable)
xai-sdk>=0.1

# PDF ingestion
PyMuPDF>=1.24
pdfplumber>=0.11

# Streamlit dashboard
streamlit>=1.30

# Dev / tests
pytest>=8.0
pytest-cov>=5.0
